<html><body>

<h1>INFO <a href="exp://host/__SERVER/servervm/sidebar.html">menu</a>  <a href="open://host/./servervm/info.vm">edit</a></h1>
 
#set ($cols = ["id","name","number","val","round","interaction","payoff","debug"])

<table border=1>
<tr>
<td><b>number</b></td>
#foreach ($c in $cols) 
   <td><b>$c</b></td>
#end
</tr>

#foreach ($dta in $data.values()) 
#set ( $goahead = true )
#foreach ($exclude in $done) 
  #if ( $exclude == $dta.id )
    #set ($goahead = false )
  #end
#end

#if ( $goahead )
#if ( $test )
    #set( $bg = "#FFFFFF" )
    #set( $test = false )
#else
    #set( $bg = "#D2D2D2" )
    #set( $test = true )
#end  
<tr>
   #set( $bg2 = $bg )
<td bgcolor="$bg"><b>$!dta.get("number") </b></td>
#foreach ($col in $cols)
   <td bgcolor="$bg">$!dta.get($col)</td>
#end
</tr>
#end
#end
</table>

#*
 * make sure the page refreshes every 10 seconds
 *#
<script>

#set( $tt = $_query.infotimeout )
#if ( !($tt) )
  #set($t = 10)
#else
  #set($I = 0)
  #set( $t = $I.parseInt($tt) )
#end
#set( $dt = 1000 * $t ) 

interval = setInterval(function() {
    var form =  document.getElementById('infochange');
    if( form.dorefresh.checked == true ) {
        clearInterval(interval);
        form.submit();
    } 
    clearInterval(interval);
}, $dt);

</script>

<form id="infochange" action="exp://host/__SERVER/servervm/info.vm">
	<input type="tel" name="infotimeout" value="$t">
	<input type="checkbox" name="dorefresh" checked='checked'>
	<input type="submit" name="refresh" value="refresh">
</form>
